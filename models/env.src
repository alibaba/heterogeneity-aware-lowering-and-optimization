# Use the src/build directory
export SRC_DIR=${PWD}/..
export BUILD_DIR=$SRC_DIR/build

export HALO_BIN=$BUILD_DIR/bin/halo
export ODLA_INC=$SRC_DIR/ODLA/include
export ODLA_LIB=$BUILD_DIR/lib

# Use the halo install directory
#export INSTALL_DIR=${CMAKE_INSTALL_PREFIX}
#export HALO_BIN=$INSTALL_DIR/bin/halo
#export ODLA_INC=$INSTALL_DIR/include
#export ODLA_LIB=$INSTALL_DIR/lib/ODLA

export TEST_TEMP_DIR=/tmp

# Test with GPU: 1 (yes) | 0 (no)
export TEST_WITH_GPU=1
