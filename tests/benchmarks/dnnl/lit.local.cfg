if not os.path.isfile(config.lib_dnnl_path):
    config.unsupported = True

device_link = ['-lodla_dnnl']
config.substitutions.append(('%device_link', ' '.join(device_link)))

config.available_features.add('dnnl')