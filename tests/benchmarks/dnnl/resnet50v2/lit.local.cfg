halo_compile_flags = ['--emit-inference-func-sig']
config.substitutions.append(('%halo_compile_flags', ' '.join(halo_compile_flags)))
model_path = ['/models/vision/classification/resnet/resnet50-v2-7.onnx']
config.substitutions.append(('%model_path', ' '.join(model_path)))

macro_flags = ['-DCOMPARE_ERROR=1e-3 -DTEST_ITERS=10']
config.substitutions.append(('%macro_flags', ' '.join(macro_flags)))

testinc_path = os.path.sep.join((config.test_source_root, 'include'))
testfile_path = os.path.sep.join((config.halo_src_dir, 'models/benchmarks'))
datafile_path = '/models/vision/classification/resnet'
link_includes = '-I %s -I %s -I %s' % (testfile_path, datafile_path, testinc_path)
config.substitutions.append(('%incs', link_includes))