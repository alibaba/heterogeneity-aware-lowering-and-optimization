if not os.path.isfile(config.lib_cudart_path):
    config.unsupported = True

device_link = ['-lodla_tensorrt']
config.substitutions.append(('%device_link', ' '.join(device_link)))

config.available_features.add('tensorrt')